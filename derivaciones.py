"""
Derivaciones (Bloque 4, Task 5 — pendiente): a partir de una pieza ganadora o
perdedora, planifica la producción de un experimento hijo ("derivar") o de una
pieza de rescate en el mismo experimento ("rescatar").

Este archivo es un stub: acciones.py lo importa a nivel de módulo para que
acciones.ejecutar pueda llamar a `planificar` (y los tests puedan
reemplazarla). Para evitar el ciclo de imports, cuando se implemente este
módulo debe importar `acciones` de forma perezosa, dentro de las funciones.
"""


def planificar(cliente, experimento_id, tipo, payload):
    """tipo ∈ ("derivar", "rescatar"). Devuelve el id de lo planificado
    (experimento hijo o pieza de rescate). Se implementa en Task 5."""
    raise NotImplementedError("derivaciones.planificar se implementa en Task 5.")
